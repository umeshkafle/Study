

<% @notes.each do |note|%><br/>
	<strong>Title: </strong>
	<%= note.title%></br>
	<strong>Content: </strong>
	<%= note.content%><br/>
	<%= link_to "Delete", subject_note_path(@subject, note.id), method: :delete, data:{confirm: "Are you sure?"} %>

<%end%>
